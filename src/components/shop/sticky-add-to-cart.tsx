"use client";

// Placeholder: full sticky-bar implementation lands in the next commit.
import type { DbWorkbookSku } from "@/lib/db-types";

export function StickyAddToCart({ book: _book }: { book: DbWorkbookSku }) {
  return null;
}
